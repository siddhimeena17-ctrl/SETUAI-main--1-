# Source Folder

This folder contains the application source for the SetuAI.org website.

Use `src/content/site.ts` for fallback content and route inventory, `src/sanity` for CMS integration, `src/components` for reusable UI, `src/lib` for shared helpers, and `src/app` for routes.
