# Components

Reusable presentation and interaction components live here.

The design system favors restrained nonprofit UI: compact buttons, icons, readable sections, accessible forms, and responsive layouts.

`site-chrome.tsx` hides the public navigation shell on admin routes and keeps it visible everywhere else.

Admin components provide the custom Upstash CMS dashboard shell, mutation buttons, and structured editor forms.
