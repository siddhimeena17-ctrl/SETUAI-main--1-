# Sanity Web Integration

Only website-facing Sanity environment helpers, clients, and loaders live here.

The editable Studio config and schemas live in `../../../studio` at the monorepo root.

The frontend should always degrade gracefully when Sanity is not configured.
