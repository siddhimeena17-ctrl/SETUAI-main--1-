# Top-Level CMS Pages

This route group holds top-level pages rendered from `src/content/site.ts` or Sanity `sitePage` documents.

The route group does not affect URLs. `/about`, `/mission`, `/impact`, `/textbook`, and similar pages are served by `[slug]`.
