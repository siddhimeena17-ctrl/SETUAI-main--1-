# Health Endpoint

`GET /api/health` is a public, no-store liveness endpoint for deployment and monitoring checks.

It intentionally returns no environment, database, subscriber, or administrative information.
