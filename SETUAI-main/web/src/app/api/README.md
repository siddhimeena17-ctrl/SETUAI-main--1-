# API Routes

Server-only route handlers live here.

Current APIs handle chatbot requests, form submissions, update subscriptions, and update notification sends. Do not expose secrets to client components.
