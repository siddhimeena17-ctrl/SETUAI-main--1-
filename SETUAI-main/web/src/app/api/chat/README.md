# Chat API

`route.ts` connects the website chatbot to Vercel AI Gateway.

The model defaults to `amazon/nova-micro` and can be changed with `AI_GATEWAY_MODEL`. The API key must come from server environment variables.
