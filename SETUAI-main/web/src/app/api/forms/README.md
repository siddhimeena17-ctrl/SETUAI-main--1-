# Forms API

`route.ts` receives school, volunteer, sponsor, and contact form submissions.

If Sanity env vars and `SANITY_API_WRITE_TOKEN` are configured, submissions are written as `formSubmission` documents.
