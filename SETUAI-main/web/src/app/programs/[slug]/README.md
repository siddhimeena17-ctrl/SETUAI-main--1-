# Dynamic Program Page

Program detail pages show audience, length, outcomes, modules, and reusable page sections.

Keep program-specific logic here minimal; update content in Sanity or `src/content/site.ts`.
