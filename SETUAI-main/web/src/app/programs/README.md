# Programs

The learning pathways index shows only reviewed, published pathways. It must not
turn a proposal or a draft into a public delivery claim.

It reads Sanity program documents when configured and falls back to `src/content/site.ts`.
