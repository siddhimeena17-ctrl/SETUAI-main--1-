# App Router

This folder uses the Next.js 16 App Router. Read `node_modules/next/dist/docs/01-app/` before changing file conventions.

Public pages live beside API routes, `sitemap.ts`, and `robots.ts`. The Sanity Studio is standalone in the root `studio/` app. Route params are promises in dynamic pages.
